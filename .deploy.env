# Namecheap Deployment Configuration
# بيانات الاتصال بالخادم

SERVER_IP=162.0.209.203
SSH_USERNAME=bitcstcp
SSH_PORT=21098

# مسارات النشر
REMOTE_PATH=/home/bitcstcp/elhamd-imports
PUBLIC_PATH=/home/bitcstcp/public_html

# إعدادات التطبيق
APP_NAME=elhamd-imports
NODE_ENV=production
PORT=3000

# أوامر النشر
DEPLOY_COMMANDS="cd $REMOTE_PATH && npm ci --only=production && npm run db:push"
START_COMMAND="cd $REMOTE_PATH && pm2 start npm --name '$APP_NAME' -- start"
RESTART_COMMAND="cd $REMOTE_PATH && pm2 restart '$APP_NAME'"
STOP_COMMAND="cd $REMOTE_PATH && pm2 stop '$APP_NAME'"

# إعدادات قاعدة البيانات
DATABASE_URL="file:./dev.db"