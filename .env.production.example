# Production Environment Variables for Vercel
# Copy these to your Vercel Environment Variables

# Database Configuration
DATABASE_URL="your-production-database-url"
PRISMA_DATABASE_URL="your-prisma-accelerate-url"

# NextAuth Configuration - CRITICAL for Vercel
NEXTAUTH_URL="https://your-domain.vercel.app"
NEXTAUTH_SECRET="your-secure-production-secret-key"

# Application Configuration
NODE_ENV="production"

# Optional: Custom Domain (if you have one)
# NEXTAUTH_URL="https://your-custom-domain.com"

# IMPORTANT: 
# 1. Generate a new secure secret: openssl rand -base64 32
# 2. Set NEXTAUTH_URL to your actual Vercel domain
# 3. Make sure DATABASE_URL is the production database URL
# 4. Deploy after setting these variables